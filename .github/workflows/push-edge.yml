name: push-edge

# Publishes the `edge` tag: the latest supported combination, built from
# master. `latest` belongs to the release workflow (ADR-0018), so this one
# never writes it.
# Triggered by pushes to master that touch the image.
on:
  push:
    branches:
      - "master"
    paths:
      - ".dockerignore"
      - ".github/workflows/push-edge.yml"
      - "Dockerfile"
      - "hadolint.yaml"
      - "security/**"
      - "supported_versions.json"
      - "tests/**"

jobs:
  build_push_edge:
    runs-on: ubuntu-22.04

    steps:
      - name: Check out the repo
        uses: actions/checkout@v4

      - name: Retrieve latest supported versions
        run: |
          echo "TF_VERSION=$(jq -r '.tf_versions | sort_by(split(".") | map(tonumber)) | .[-1]' supported_versions.json)" >> $GITHUB_ENV
          echo "AWS_VERSION=$(jq -r '.awscli_versions | sort_by(split(".") | map(tonumber)) | .[-1]' supported_versions.json)" >> $GITHUB_ENV

      - name: Login to Docker Hub registry
        run: echo '${{ secrets.DOCKERHUB_PAT }}' | docker login -u ${{ secrets.DOCKERHUB_USERNAME }} --password-stdin

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Buildx
        id: buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and push container images
        uses: docker/build-push-action@v6
        with:
          context: .
          platforms: linux/amd64,linux/arm64
          build-args: |
            TERRAFORM_VERSION=${{ env.TF_VERSION }}
            AWS_CLI_VERSION=${{ env.AWS_VERSION }}
          tags: bgauduch/terraform-aws-cli:edge
          push: true
          cache-from: type=gha
          cache-to: type=gha,mode=max
